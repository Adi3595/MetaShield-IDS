# Contributing
Aditya Gawali

Thank you for your interest in MetaShield IDS.

## How to run locally

1. Create and activate a virtual environment.
2. Install dependencies:

```bash
pip install -r requirements.txt
```

3. Start the API:

```bash
python api.py
```

4. (Optional) Run tests:

```bash
pytest
```

## Notes

- Avoid committing generated artifacts such as `checkpoints/`, `results/`, and `logs/`.
- If you add new dependencies, update `requirements.txt`.

